# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Svelte TypeScript CI

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: Install Dependencies
        run: npm ci

      - name: Build
        env:
          BASE_PATH: ''
        run: |
          npm run build
          touch build/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./build

      - name: CI Unit Tests
        run: npm run test:ci

  e2e:
    runs-on: ubuntu-latest
    services:
      frequency-instant:
        image: 'frequencychain/standalone-node:v2.0.0-rc1'
        ports:
          - '9944:9944' # ws and rpc port
        options: >-
          --health-cmd="bash -c "curl -X POST -H \"Content-Type: application/json\" -d '{\"jsonrpc\":\"2.0\",\"method\":\"system_health\",\"params\":[],\"id\":1}' http://127.0.0.1:9944 > /dev/null 2>&1"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci
      - name: 'E2E Tests'
        run: npm run test:e2e

  # Preview PRs when actions/deploy-pages preview option is in public
  # https://github.com/actions/deploy-pages/issues/180
  # deploy-preview:
  #   # Skip unless it is an open PR
  #   if: github.event_name == 'pull_request' && github.event.action != 'closed'
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
  #   permissions:
  #     contents: read
  #     pages: write
  #     id-token: write
  #   runs-on: ubuntu-latest
  #   needs: build

  #   steps:
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v4
  #       with:
  #         preview: 'true'
